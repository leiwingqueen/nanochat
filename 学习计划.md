# nanochat 实践学习计划

> 基于你已完成的 Karpathy Zero to Hero 系列（micrograd、makemore、backprop、WaveNet）制定，目标是用 4-5 周把 nanochat 仓库吃透，并动手训练/魔改出自己的一版模型。

---

## 前置准备（Day 0）

- **硬件**：不需要一开始就上 8xH100。先在单卡（哪怕是消费级显卡）或云端单卡实例上用小 `--depth`（如 d12、d20）跑通，费用可控在几美元到几十美元。等流程跑熟了再考虑租 8xH100 跑 speedrun 全流程（约 $24/hr，几小时内可完成 GPT-2 级别的训练）。
- **账号/工具**：GitHub 账号、`uv`（Python 项目管理工具，仓库用它管理依赖）、一个能跑 CUDA 的环境。
- **必读起点**：GitHub Discussion #1《Introducing nanochat》——Karpathy 逐段讲解了 `speedrun.sh`，相当于官方代码导读，比自己盲读源码效率高得多。

---

## 第 1 周：跑通 + 建立整体地图

**目标：不求全懂，先让流水线在你手里跑起来一次，建立"预训练 → mid-training → SFT → RL → 推理"的整体框架感。**

- Day 1-2：clone 仓库，用小 depth（d12 最快，几分钟能训完）跑一遍 `speedrun.sh`，全程盯着日志，搞清楚每个阶段大概花多久、产出什么文件。
- Day 3：读 Discussion #1，对照你刚跑的日志逐段核对，标出你还看不懂的术语/步骤。
- Day 4-5：画一张自己的流水线图（数据下载 → tokenizer 训练 → 预训练 → mid-training → SFT → RL → chat_web 推理），标注每一步输入输出是什么文件、存在哪里（`~/.cache/nanochat`）。
- 周末：用训练好的 d12 模型跑 `scripts.chat_web`，和自己训的模型对话，建立成就感锚点。

**产出检验**：能不看文档，口头讲清楚 nanochat 的 5 个训练阶段分别在做什么。

---

## 第 2 周：模型与 tokenizer 代码精读

**目标：把 `nanochat/gpt.py` 和 tokenizer 部分吃透——这部分是你 makemore/WaveNet 知识的直接延伸。**

- Day 1-2：精读 `nanochat/gpt.py`。带着问题读：
  - 这里的自注意力实现和你在 Zero to Hero 里手写的 micrograd/makemore 版本比，工程上多了什么（KV cache、flash attention、旋转位置编码等）？
  - `--depth` 这一个整数是怎样通过 aspect ratio 推导出宽度、头数、学习率的？对照 Discussion #420（miniseries v1）里的 scaling law 推导。
- Day 3-4：精读 tokenizer 训练代码，对比你之前在 makemore 里做的字符级 bigram tokenizer——理解从字符级到 BPE 子词级的跨越解决了什么问题（词表效率、OOV、序列长度）。
- Day 5：动手小实验——改一下 tokenizer 词表大小或 `--depth`，重跑 d12，观察 CORE score 变化，记录下来。

**产出检验**：能画出 `--depth` 如何联动推出所有其他超参数的推导链条。

---

## 第 3 周：预训练循环 + 数据管线

**目标：搞懂训练循环本身、数据 shard 加载、优化器和学习率调度——这是 makemore 系列里"迷你版"内容的工业级实现。**

- Day 1-2：读数据加载部分。理解为什么用 shard 化的 parquet 而不是 `datasets.load_dataset()`，思考这和你在 Kugou 处理大规模指标数据分片、避免单点瓶颈的思路有没有相通之处（其实是同一类工程问题：大数据集的可扩展 I/O）。
- Day 3-4：读优化器和学习率调度代码，对照 Discussion #420 里的 CORE score 拟合公式，理解"compute optimal"是怎么被量化出来的。
- Day 5：动手实验——换一种学习率调度策略（比如改 warmup 步数），跑 d12 或 d20，对比 loss 曲线。

**产出检验**：能解释"为什么改一个 depth 数字，训练时长和最终效果会按预期变化"。

---

## 第 4 周：mid-training / SFT / RL —— 新知识区

**目标：这是 Zero to Hero 系列没有覆盖的部分，重点放慢速度。**

- Day 1-2：读 mid-training 代码，理解它和预训练的区别（数据分布、目标是让模型"学会对话格式"而非单纯续写）。
- Day 3-4：读 SFT（监督微调）代码，理解指令数据格式、损失只在 assistant 回复上计算等细节。
- Day 5-7：读 RL 部分（如果你之前没接触过 RLHF/GRPO 之类的概念，建议先花半天看一个 RL 微调的通俗讲解视频/文章打底，再回来读代码），重点搞懂：
  - reward 是怎么定义的（GSM8K 之类的可验证任务）
  - 相比 SFT，RL 阶段的训练循环结构有什么本质不同

**产出检验**：能说清楚 pretraining → mid-training → SFT → RL 这四个阶段，每一步分别改变了模型的什么能力。

---

## 第 5 周（可选，进阶）：自己动手改一处并跑一次完整 speedrun

**目标：不再是读代码，而是真正贡献一个改动。**

- 从下面选一个方向做一次小实验，跑完整个流程，写一份类似 Discussion 里那种"实验记录"：
  - 换一种 tokenizer 策略或词表大小
  - 调整数据配比或数据源
  - 改进优化器（比如换一种权重衰减策略）
  - 尝试更大的 `--depth`（如果预算允许，租一次 8xH100 跑几小时的 speedrun）
- 把结果和 baseline 的 CORE score 对比，形成一页总结。

---

## 辅助资源清单

- GitHub Discussion #1：speedrun.sh 逐段讲解（最重要的起点）
- GitHub Discussion #420：miniseries v1，depth → 超参数的 scaling law 推导
- GitHub Discussion #365：有人问过"如何系统学习 nanochat 代码"，Karpathy 回复正在准备讲解视频，值得持续关注
- `#nanochat` Discord 频道 / DeepWiki（针对该仓库的 AI 问答工具）：卡壳时用来快速定位代码逻辑

---

## 一些提醒

- 不要在第 1-2 周就追求"完全读懂每一行"，先建立框架感，细节在动手实验中自然会补上。
- 每周末留一点时间做"动手小实验"而不是纯阅读——nanochat 单次训练成本低（d12 几分钟、d20 也就几十分钟到小时级），这是它最适合用来做实验式学习的地方。
- 如果 RL 部分卡住，可以先跳过深挖细节，把 SFT 之前的部分吃透，RL 单独作为后续专题（可以结合更系统的 RLHF/GRPO 资料）。